services:
  - type: web
    name: movie-recommender
    runtime: python
    buildCommand: pip install -r movie-rec/requirements.txt
    startCommand: python -m uvicorn movie-rec.backend.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
